interface HeaderProps {
  setBackgroundColor: (language: string) => void
  backgroundColor: string
  color: string
  setColor: (language: string) => void
}

/*language: string
setLanguage: (language: string) => void*/
